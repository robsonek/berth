# Binaries
/berth
/berth.exe
/dist/

# Go
*.test
*.out
coverage.*

# berth local state — never commit secrets
.berth/
*.secrets*

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local-only — maintainer notes, real server configs, and the test app we deploy
# against (must NEVER reach this public repo)
/CLAUDE.md
/servers/
