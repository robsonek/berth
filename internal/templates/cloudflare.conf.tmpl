{{ range .Ranges -}}
set_real_ip_from {{ . }};
{{ end -}}
real_ip_header CF-Connecting-IP;

geo $realip_remote_addr $berth_cloudflare {
    default 0;
{{- range .Ranges }}
    {{ . }} 1;
{{- end }}
}
